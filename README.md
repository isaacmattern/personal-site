This is the readme for my personal site....

`npm run dev` to run locally

`npm run build` then `npm start` to fully build and start.

Just read my blog if you want to know how this thing works, but basically it generates posts based on MDX (markdown plus react) using a nice lil library called contentlayer2.

Like and subscribe.
